services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/uppass
      - DEK_KEY=${DEK_KEY}
      - PRIVATE_KEY_PATH=/app/certs/private_key.pem
    volumes:
      - ./certs:/app/certs
    networks:
      - internal
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=uppass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal
      - public
    ports:
      - "5432:5432"

  ingress:
    build:
      context: ./nginx
    ports:
      - "80:80"
      - "443:443"
    networks:
      - public
      - internal
    depends_on:
      - backend

networks:
  internal:
    internal: true
  public:


volumes:
  postgres_data:
